# Adding indexes for all forign keys

```
% rails g migration AddIndexes
      invoke  active_record
      create    db/migrate/20130514162344_add_indexes.rb
```

Should probably be done with the migration in which they were added.
